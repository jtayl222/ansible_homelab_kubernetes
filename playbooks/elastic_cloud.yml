- name: Install ECK Operator
  hosts: localhost
  connection: local
  gather_facts: false

  vars_files:
    - ../group_vars/all/common.yml

  roles:
    - elastic_operator
    - elasticsearch
    - kibana
    - filebeat
