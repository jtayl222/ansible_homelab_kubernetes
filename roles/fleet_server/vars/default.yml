---
# Default variables for Fleet server

# Namespace where ELK stack is deployed
elk_namespace: "elk-stack"

# Elasticsearch settings
elasticsearch_cluster_name: "k3s-elk"

# Fleet version (should match Elasticsearch version)
fleet_version: "8.5.1"

# Traefik settings
traefik_port: 30080